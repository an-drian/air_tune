<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>AirTune · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header class="navbar navbar-expand-md navbar-dark bd-navbar">
      <section class="container">
        <nav role="navigation" class="main-nav">
          <%= if @current_user do %>
            <ul>
              <li>
                <%= link "Logout", to: Routes.pow_session_path(@conn, :delete), method: :delete, class: "nav-link" %>
              </li>
              <li><%= @current_user.email %></li>
            </ul>
          <% else %>
            <ul>
              <li>
                <%= link "Sign up", to: Routes.pow_registration_path(@conn, :new), class: "nav-link" %>
              </li>
              <li>
                <%= link "Log in", to: Routes.pow_session_path(@conn, :new), class: "nav-link" %>
              </li>
            </ul>
          <% end %>
        </nav>
        <%= link "", to: Routes.home_path(@conn, :index), class: "logo nav-link" %>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
